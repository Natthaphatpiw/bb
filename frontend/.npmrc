# Ensure lockfile is used
package-lock=true

# Strict peer dependencies
legacy-peer-deps=false

# Engine strict to prevent version mismatches
engine-strict=true

# Save exact versions
save-exact=false

